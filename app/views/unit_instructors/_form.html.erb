<%= form_with(model: unit_instructor, local: true) do |form| %>
  <% if unit_instructor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unit_instructor.errors.count, "error") %> prohibited this unit_instructor from being saved:</h2>

      <ul>
      <% unit_instructor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :delivery_id %>
    <%= form.number_field :delivery_id, id: :unit_instructor_delivery_id %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :unit_instructor_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
