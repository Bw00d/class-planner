<%= form_with(model: cost, local: true) do |form| %>
  <% if cost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cost.errors.count, "error") %> prohibited this cost from being saved:</h2>

      <ul>
      <% cost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :price %>
    <%= form.text_field :price, id: :cost_price, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :delivery_id %>
    <%= form.number_field :delivery_id, id: :cost_delivery_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :user_id %>
    <%= form.number_field :user_id, id: :cost_user_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :comment %>
    <%= form.text_area :comment, id: :cost_comment, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :date %><br>
    <%= form.date_select :date, id: :cost_date, class: "form-control" %>
  </div>

  <div class="actions">
    <%= form.submit "SUBMIT", class: "btn btn-success" %>
  </div>
<% end %>
